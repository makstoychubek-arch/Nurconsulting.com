# Итог: деплой `autobidder-tick`

09.09.2026. CI PR #84 зелёный → `main` `cabb936`.

## Продакшен

| Что | Статус |
|---|---|
| Edge Function `autobidder-tick` | задеплоена |
| pg_cron `autobidder_tick` | `*/5 * * * *`, active |
| `DRY_RUN` | **true** (ставки в WB не уходят) |
| `autobidder_rules` v2 | **0 строк** — тик пустой, журнал не пишется |

## Проверка HTTP

| Вызов | Результат |
|---|---|
| POST без Bearer | 401 |
| POST с service_role, `{}` | 200, `dry_run: true`, 4 кабинета, `decisions: 0` у каждого |

Кабинеты в ответе: Baza, Elium, Zevina 1, Zevina 2. Токены из Vault читаются, правил нет — решений нет.

Дата в ответе — календарный день Бишкека (UTC+6).

## Как не включить боевые ставки случайно

Cron шлёт `{}` → `parseDryRun` даёт `true`.  
Живой `setBids` только если в теле `"dry_run": false` **или** секрет функции `DRY_RUN=false`. Пока не включать.

## Дальше

Пока нет правил v2, каждые 5 минут функция просто обходит кабинеты и выходит.  
Когда появятся правила — в `bid_history` будут решения с `applied=false` и `pos_unknown` (заглушка позиции), без записи в WB.
